{
  "name": "jxchess",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "JXchess v0.1 复盘棋盘：PGN 导入、逐步回放、摆棋研究",
  "author": "谏贤 (JxAries)",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "app": "tauri dev",
    "stage:webview2": "node scripts/stage-webview2-loader.mjs",
    "package": "npm run stage:webview2 && tauri build",
    "package:setup": "npm run stage:webview2 && tauri build --bundles nsis",
    "package:msvc": "node scripts/stage-webview2-loader.mjs --target x86_64-pc-windows-msvc && tauri build --target x86_64-pc-windows-msvc --bundles nsis"
  },
  "dependencies": {
    "chess.js": "^1.4.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  }
}
